import React from "react";
import MessageHistory from "./components/MessageHistory";

const Message = () => {
  return (
    <div>
      <MessageHistory />
    </div>
  );
};

export default Message;
